import { TenderlyService } from "@tenderly/api-client";
import { VerifyContractABIResponse } from "@tenderly/api-client";
import { HardhatRuntimeEnvironment } from "hardhat/types";
import { TenderlyNetwork } from "../TenderlyNetwork";
export declare class VerificationService {
    private readonly tenderlyService;
    private readonly tenderlyNetwork;
    constructor(tenderlyService: TenderlyService, tenderlyNetwork: TenderlyNetwork);
    verifyContractABI(hre: HardhatRuntimeEnvironment, address: string, contractName: string): Promise<VerifyContractABIResponse>;
    private makeVerifyContractABIRequest;
}
//# sourceMappingURL=service.d.ts.map